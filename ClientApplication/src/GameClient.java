public class GameClient { //considering making this a singleton, since each client should be unique in its own instance

    //Empty, will be filled with the functions to read keyboard input and send to server

    //will need a function to act as main
}
